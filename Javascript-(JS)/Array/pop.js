// pop
        let b = []; // can be write on this 
        b[0] = "Farhana";;
        b[1] = "Shetu";;
        b[2] = "is student of IDB";
        console.log(b.length); // Returns number of elements.
        b.pop(); // Removes the last element.
        console.log(b);