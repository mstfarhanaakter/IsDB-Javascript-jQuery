// splice method is used to add, remove, or replace   (start, deleteCount, item1, item2, . ..)
        // elements in an array in place (it modifies the original array).

        let fruits = ["Apple", "Banana", "date", "fig"]
        fruits.splice(1, 1, "Juice") // start, delete, insert
        console.log(fruits)
