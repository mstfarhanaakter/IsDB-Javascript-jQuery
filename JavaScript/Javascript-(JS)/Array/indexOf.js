// In JavaScript, the indexOf() method is used to find the first occurrence
// of a specified value within a string or an array. 
// It returns the index of the found item, or -1 if the item is not found.

let fruit = ["apple", "papaya", "banana", "Date", "Mango", 4, 5, 7, 7.8];
        let index = fruit.indexOf("papaya");
        console.log(index)
        
        
var hey = "Bangladesh";
var find = hey.indexOf("n");
console.log(find);